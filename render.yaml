# Déploiement Render, même forme que les autres serveurs du portfolio.
services:
  - type: web
    name: passeur-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
